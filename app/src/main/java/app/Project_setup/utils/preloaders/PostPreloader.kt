package app.Project_setup.utils.preloaders

//, private val postAdapter: PostAdapter

/*
class PostPreloader(private val glide: RequestManager) : ListPreloader.PreloadModelProvider<String?> {
    override fun getPreloadItems(position: Int): List<String?> {
        return postAdapter.getMediaByPosition(position)
    }

    override fun getPreloadRequestBuilder(item: String): RequestBuilder<*> {
        return glide
            .load(item)
            .override(1024, 768)
    }
}*/
